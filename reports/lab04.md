# Лабораторна робота № 4

## Титульна сторінка

**Факультет:** ФІТ, КНУ  
**Дисципліна:** Кросплатформне програмування  
**Рік:** 2026  
**Тема:** "Гра Криниця-Ножиці-Папір"  
**Автор:** Roman Koshchei  
**Курс навчання:** [вкажіть курс]  
**Група:** [вкажіть номер групи]  
**Викладач:** [вкажіть ПІБ викладача]

## Постановка задачі

Потрібно створити Java-застосунок для гри "Криниця-Ножиці-Папір", де користувач грає проти комп'ютера. Комп'ютер робить хід після вибору користувача, але не використовує поточний хід користувача для шахрайського вибору.

Правила перемоги:

- криниця перемагає ножиці;
- ножиці перемагають папір;
- папір перемагає криницю;
- якщо ходи однакові, раунд переграється.

Застосунок має підтримувати задану користувачем кількість сеансів, три режими комп'ютера, запис результатів у файл і перегляд статистики перемог та поразок.

## Мета

Реалізувати консольну гру "Криниця-Ножиці-Папір" з кількома стратегіями комп'ютера, збереженням історії та статистикою результатів.

## Реалізація

Створено Maven-модуль `lab04`, який наслідує кореневий parent `pom`. Реалізовано правила гри, три стратегії комп'ютера, збереження завершених сеансів у CSV-файл, читання історії та обчислення статистики. `main` запускає коротку демонстрацію без зависання, а також підтримує інтерактивний режим через аргумент `--interactive`.

В інтерактивному режимі користувач перед початком задає кількість сеансів і обирає режим комп'ютера: випадковий, адаптивний до поточного сеансу або адаптивний до історії з файлу. Якщо ходи однакові, результат вважається нічиєю, сеанс не зараховується як завершений і переграється. У меню є опція `s`, яка показує статистику перемог і поразок.

Ключові компоненти:

- `Move` описує ходи й правила визначення переможця.
- `RandomStrategy` виконує випадковий вибір ходу.
- `SessionAdaptiveStrategy` адаптується до ходів гравця в межах поточного сеансу.
- `FileAdaptiveStrategy` адаптується до збереженої історії з CSV-файлу.
- `HistoryStore` записує й читає історію завершених сеансів та статистику.
- `GameSession` проводить раунди без шахрайства зі сторони стратегії.
- `Lab04Application` забезпечує демонстраційний та інтерактивний запуск.

## Програмний код з поясненнями

Фрагмент визначення переможця:

```java
public Outcome playAgainst(Move other) {
    if (this == other) {
        return Outcome.DRAW;
    }
    if ((this == WELL && other == SCISSORS)
            || (this == SCISSORS && other == PAPER)
            || (this == PAPER && other == WELL)) {
        return Outcome.PLAYER_WIN;
    }
    return Outcome.COMPUTER_WIN;
}
```

Метод порівнює хід гравця з ходом комп'ютера. Спочатку обробляється нічия, потім явно перелічені три виграшні комбінації гравця. Якщо жодна з них не виконалась, перемагає комп'ютер.

Фрагмент запису історії:

```java
public void append(Round round) throws IOException {
    Path parent = path.getParent();
    if (parent != null) {
        Files.createDirectories(parent);
    }
    if (Files.notExists(path)) {
        Files.writeString(path, HEADER + System.lineSeparator());
    }
    String line = round.playerMove().name() + ","
            + round.computerMove().name() + ","
            + round.outcome().name()
            + System.lineSeparator();
    Files.writeString(path, line, StandardOpenOption.APPEND);
}
```

Історія зберігається у CSV-форматі. Перед записом створюється батьківська директорія, а для нового файлу додається заголовок. Ці дані потім використовуються для статистики та адаптивної стратегії, що аналізує попередні сеанси.

## Тестування

JUnit 5 тести перевіряють:

- правила гри;
- роботу адаптивної стратегії для поточного сеансу;
- адаптацію до історії з файлу;
- коректний підрахунок статистики;
- те, що нічия не записується як завершений сеанс.

Команда запуску тестів:

```bash
mvn -q -pl lab04 test
```

## Запуск

Демонстрація:

```bash
java -cp lab04/target/classes ua.edu.unicrossplatform.lab04.Lab04Application
```

Інтерактивний режим, випадкові ходи:

```bash
java -cp lab04/target/classes ua.edu.unicrossplatform.lab04.Lab04Application --interactive
```

Інтерактивний режим, адаптація до поточного сеансу:

```bash
java -cp lab04/target/classes ua.edu.unicrossplatform.lab04.Lab04Application --interactive session
```

Інтерактивний режим, адаптація до історії:

```bash
java -cp lab04/target/classes ua.edu.unicrossplatform.lab04.Lab04Application --interactive history
```

## Висновки

Отримано завершену консольну гру з кількома стратегіями, файловою історією, статистикою та тестами для основної логіки. Реалізація відокремлює правила гри, стратегії та збереження історії, тому окремі частини можна перевіряти незалежно.
